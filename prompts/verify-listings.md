# Verify listings

Paste to your agent:

> Re-check every `<slug>` listing in `tracker/data.json` with status
> `submitted` or `pending-review`. Load each platform and find whether the
> public listing is now visible — check the product-page pattern in
> `platforms/platforms.csv`, the platform's search, and any confirmation
> email/dashboard state I point you to. A listing is `live` only when the
> public URL loads with the product on it — record `listing_url`, `live_at`,
> and evidence. If it's still queued, keep the status and set a `next_action`
> date. Report what changed.
