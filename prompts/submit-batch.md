# Submit a batch

Paste to your agent:

> Submit `<slug>` to the approved batch: <platform-slugs>. For each one: read
> the playbook, verify every `UNVERIFIED` field on the live site, fill the
> form strictly from `products/<slug>/brief.md`, and submit only when nothing
> needs my input. Stop and ask me at any account creation, payment, email
> verification, CAPTCHA, or public post. After each platform: update
> `tracker/data.json` per `docs/evidence.md` and write everything you learned
> back into the playbook. When the batch is done, give me the status table.
