# Report

Paste to your agent:

> Read `tracker/data.json` and give me the campaign status for `<slug>`:
> counts by status, every `live` listing with its URL, everything blocked on
> me (accounts, payments, CAPTCHAs) with exactly what you need, and anything
> `rejected` with the observed reason. Mirror the tracker exactly — if a
> status is `pending-review`, say pending review.
